// ปุ่มกลับหน้าหลัก
function goHome() {
  alert("กลับไปหน้าแรก (ใส่ลิงก์ได้)");
  // ตัวอย่าง: window.location.href = "home.html";
}

// ปุ่มเสร็จสิ้น
function finishForm() {
  alert("บันทึกข้อมูลเรียบร้อย");
  // คุณสามารถเพิ่มโค้ดส่งข้อมูลไปยัง backend ได้ที่นี่
}
